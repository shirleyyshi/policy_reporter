# 开发/测试依赖（生产环境无需安装）
# 安装：pip install -r requirements-dev.txt

-r requirements.txt

# pytest 核心
pytest~=8.0.0
pytest-django~=4.9.0
pytest-cov~=5.0.0
coverage~=7.5.0

# 测试辅助
factory-boy~=3.3.0      # 工厂模式造测试数据
responses~=0.25.0       # mock requests HTTP 调用
